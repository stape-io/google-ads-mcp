apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "google-ads-mcp.fullname" . }}-test-connection"
  labels:
    {{- include "google-ads-mcp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "google-ads-mcp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
